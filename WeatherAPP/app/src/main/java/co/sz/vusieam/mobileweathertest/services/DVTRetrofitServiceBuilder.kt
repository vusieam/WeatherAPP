package co.sz.vusieam.mobileweathertest.services
/*

import co.sz.vusieam.mobileweathertest.utils.ApiEndpointConstants
import okhttp3.OkHttpClient
import retrofit2.Retrofit
import retrofit2.converter.gson.GsonConverterFactory

object DVTRetrofitServiceBuilder {
    private val okHttp: OkHttpClient.Builder = OkHttpClient.Builder()
    private val builder: Retrofit.Builder = Retrofit.Builder().baseUrl(ApiEndpointConstants.toString())
        .addConverterFactory(GsonConverterFactory.create()).client(okHttp.build())
    private val retrofit: Retrofit = builder.build()
    fun <T> buildWeatherService(serviceType:Class<T>):T{
        return retrofit.create(serviceType)
    }
}*/
